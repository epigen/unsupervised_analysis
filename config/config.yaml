# always use absolute paths
# provide at least one parameter per option (no empty parameters allowed)

##### RESOURCES #####
# memory in MB
mem: '32000'
mem_small: '16000'
threads: 2
partition: 'shortq'

##### GENERAL #####
annotation: /research/home/sreichl/projects/macroIC/config/AKsmall_unsupervised_analysis_annotation.csv
result_path: /nobackup/lab_bock/projects/macroIC/results/AKsmall/
project_name: AKsmall

##### PCA #####
# (no configuration necessary)

##### UMAP #####
# information on the various parameters: https://umap-learn.readthedocs.io/en/latest/parameters.html
# umap knn-graph will be generated for each metric once with the max(n_neighbors)
# knn-graph parameters: metric  (default: euclidean), n_neighbors (default: 15)
# embedding parameters: min_dist  (default: 0.1), n_components (default: 2)
# densmap flag: perform densMAP  (0==no, 1==yes) on top of UMAP with all parameter combinations (https://umap-learn.readthedocs.io/en/latest/densmap_demo.html)
# connectivity visualization flag (0==no, 1==yes), computational expensive (slow), recommendation 0 for exploration and 1 for validation
umap:
    metrics: ['euclidean','correlation']
    n_neighbors: [10]
    min_dist: [0.1]
    n_components: [2]
    densmap: 0
    connectivity: 0
    diagnostics: 0
    
##### VISUALIZATION #####
# 2D visualization with ggplot2
scatterplot2d:
    size: 0.5
    alpha: 1
    
# metadata column used for plotting of sequential PCs,... (or empty "", then first column of metadata data frame will be taken)
metadata_of_interest: "target"

# specify features of interest, which value from the data will be highlgihted in the 2D plots
# motivated by bioinformatics highlighting expression levels of marker genes (eg: ['PTPRC'])
# if not used leave empty []
features_to_plot: []


# interactive (2D/3D) visualization with ???

##### LEIDEN CLUSTERING #####


##### HDBSCAN CLUSTERING #####